import React from 'react';
import classes from './About.module.scss';
import Layout from '../Layout';

const About = () => (
  <Layout>
    <div className={classes.root}>O nama</div>
  </Layout>
);

export default About;
